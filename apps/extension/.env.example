# Copy to .env. These are PUBLIC values (no secrets) — the OAuth client id is embedded in every
# build of the extension. .env is git-ignored; this template is committed.
VITE_API_BASE=http://localhost:8080
VITE_OAUTH_CLIENT_ID=your-web-oauth-client-id.apps.googleusercontent.com
