# Kotiq Guard — know before you `npm install`

Scan a repo/project for malicious install hooks **before** you run it.
Cloud (Chrome extension + Cloud Run + Gemini) and local (CLI + Ollama/Gemma) — one engine.