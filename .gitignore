# dependencies
node_modules/

# secrets — NEVER commit
.env
.env.*
!.env.example

# build output
dist/
*.tsbuildinfo

# editors / tooling
.claude
.idea
.vscode

# terraform — state can hold sensitive values; .terraform/ is downloaded plugins.
# NOTE: .terraform.lock.hcl is intentionally NOT ignored — it must be committed.
.terraform/
*.tfstate
*.tfstate.*
crash.log

# os
.DS_Store
